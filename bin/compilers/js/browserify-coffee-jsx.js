// Generated by CoffeeScript 1.7.1
(function() {
  var BrowserifyCoffeeJSXCompiler, BrowserifyCompiler, browserify, browserify_shim, coffee_reactify, fs,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  fs = require('fs');

  browserify = require('browserify');

  coffee_reactify = require('coffee-reactify');

  browserify_shim = require('browserify-shim');

  BrowserifyCompiler = require('metaserve/src/compilers/js/browserify').BrowserifyCompiler;

  require('node-cjsx').transform();

  BrowserifyCoffeeJSXCompiler = (function(_super) {
    __extends(BrowserifyCoffeeJSXCompiler, _super);

    function BrowserifyCoffeeJSXCompiler() {
      return BrowserifyCoffeeJSXCompiler.__super__.constructor.apply(this, arguments);
    }

    BrowserifyCoffeeJSXCompiler.prototype.default_options = {
      base_dir: './static/js',
      ext: 'coffee',
      browserify: {
        extensions: ['.coffee']
      }
    };

    BrowserifyCoffeeJSXCompiler.prototype.beforeBundle = function(bundler) {
      return bundler.transform(coffee_reactify).transform(browserify_shim);
    };

    return BrowserifyCoffeeJSXCompiler;

  })(BrowserifyCompiler);

  module.exports = function(options) {
    if (options == null) {
      options = {};
    }
    return new BrowserifyCoffeeJSXCompiler(options);
  };

}).call(this);
